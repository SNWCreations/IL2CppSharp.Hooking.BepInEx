# Third-Party Notices

This project does not vendor third-party source code. It references NuGet packages for compile-time integration.

## NuGet Dependencies

- `IL2CppSharp.Hooking` is licensed as `MIT`.
- `IL2CppSharp` is licensed as `Apache-2.0`.
- `BepInEx.Core` is licensed as `LGPL-2.1-only` in its NuGet metadata.
- `SNWCreations.Il2CppInterop.Runtime` and its `SNWCreations.Il2CppInterop.Common` dependency are based on BepInEx `Il2CppInterop` and are licensed as `LGPL-3.0-only`.

Review the licenses of these packages before redistributing binaries that include or bundle them.
